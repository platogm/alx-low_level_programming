#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>

/**
 * positive_or_negative - checks if the number is pos or not
 * @i: the number to be checked
 * Return: nothing
 */
void positive_or_negative(int i);

int largest_number(int a, int b, int c);

#endif /* MAIN_H */
